import React from 'react';
import '../index.css';

function Navbar() {
    return (
        <nav className="navbar">
            <ul className="navbar-nav"></ul>
        </nav>
    );
}

export default Navbar;